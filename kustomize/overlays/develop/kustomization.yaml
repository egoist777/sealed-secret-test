apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../base

namespace: develop

generators:
  - secret-generator.yaml

configMapGenerator:
- name: mongo-cm
  literals:
  - mongo-server=mongodb-service